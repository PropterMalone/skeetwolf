/**
 * Feed generator stub.
 * Will be a Cloudflare Worker that serves per-game custom feeds
 * based on labeler-applied labels.
 *
 * Implementation deferred until:
 * 1. Game engine MVP is working
 * 2. Labeler is applying game labels to posts
 */
export {};
